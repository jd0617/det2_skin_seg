MODEL:
  META_ARCHITECTURE: "HRNet_SEG"
  BACKBONE:
    NAME: "build_hrnet_backbone"
  NUM_MASKS: 15
  ALIGN_CORNERS: False
  MODEL_WEIGHTS: ""
  HRNET:
    STEM_OUT_CHANNELS: 64
    # NORMALIZE_METHOD: "BN"
    # STEM_INPLANES: 64
    # STAGE1:
    #   NUM_MODULES: 1
    #   NUM_BRANCHES: 1
    #   BLOCK: "BOTTLENECK"
    #   NUM_BLOCKS: [4]
    #   NUM_CHANNELS: [64]
    #   FUSE_METHOD: "SUM"
    STAGE2:
      NUM_MODULES: 1
      NUM_BRANCHES: 2
      BLOCK: BASIC
      NUM_BLOCKS:
      - 4
      - 4
      NUM_CHANNELS:
      - 32
      - 64
      FUSE_METHOD: SUM
    STAGE3:
      NUM_MODULES: 4
      NUM_BRANCHES: 3
      BLOCK: BASIC
      NUM_BLOCKS:
      - 4
      - 4
      - 4
      NUM_CHANNELS:
      - 32
      - 64
      - 128
      FUSE_METHOD: SUM
    STAGE4:
      NUM_MODULES: 3
      NUM_BRANCHES: 4
      BLOCK: BASIC
      NUM_BLOCKS:
      - 4
      - 4
      - 4
      - 4
      NUM_CHANNELS:
      - 32
      - 64
      - 128
      - 256
      FUSE_METHOD: SUM

INPUT:
  FORMAT: "RGB"
  MIN_SIZE_TRAIN: (600,)
  MAX_SIZE_TRAIN: 1200
  MIN_SIZE_TEST: 600
  MAX_SIZE_TEST: 1200

SOLVER:
  BASE_LR: 0.002
  MAX_ITER: 0
  STEPS: [0, 0]
  IMS_PER_BATCH: 16
  GAMMA: 0.99
  CHECKPOINT_PERIOD: 1000
  MOMENTUM: 0.9
  WEIGHT_DECAY: 0.0001
  OPTIMIZER: 'Adam'

